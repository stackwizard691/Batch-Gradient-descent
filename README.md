# Batch-Gradient-descent
This repository contains an implementation of Linear Regression built from scratch using Batch Gradient Descent. The project focuses on understanding how model parameters are optimized by minimizing the cost function using the entire training dataset in each iteration.  
